# Домены для ручного исключения из zapret2
# Формат: один домен на строку
# Субдомены включаются автоматически (example.ru = *.example.ru)
# ^ перед доменом = строгое совпадение (^example.ru = только example.ru)
# Строки с # в начале — комментарии
